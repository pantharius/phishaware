{
  "name": "phishingsite",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:ui": "cd ui && npm run dev",
    "dev:api": "cd api && npm run start:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
